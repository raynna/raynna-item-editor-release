# Launcher

Use `launch-item-editor.bat` to install or update the latest packaged editor from this release repository.

This launcher downloads only packaged builds. It does not download the editor source code.
