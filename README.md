# Raynna Item Editor Releases

This repository is the publish target for the editor launcher.

It contains:
- `manifest.json` for update metadata
- `releases/` packaged editor zip builds
- `launcher/` bootstrap files for end users

It does not contain the editor source code.
